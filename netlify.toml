[build]
  base = "evidence"
  command = "npm run sources && npm run build"
  publish = ".evidence/template/build"

[build.environment]
  # This forces the AWS SDK to see the region regardless of Netlify's restrictions
  AWS_REGION = "eu-central-1"
  AWS_DEFAULT_REGION = "eu-central-1"
  NODE_VERSION = "18"